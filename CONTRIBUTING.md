# Contributing to Bookmark Manager

感谢你考虑为 Bookmark Manager 做出贡献！

## 如何贡献

### 报告 Bug

如果你发现了 bug，请创建一个 issue 并包含：

- 清晰的标题和描述
- 重现步骤
- 预期行为和实际行为
- 截图（如果适用）
- 你的环境信息（操作系统、版本等）

### 提出新功能

如果你有新功能的想法：

1. 先创建一个 issue 讨论这个功能
2. 等待维护者的反馈
3. 如果获得批准，可以开始实现

### 提交代码

1. **Fork 项目**
   ```bash
   # 克隆你的 fork
   git clone https://github.com/hacker4257/bookmark-manager.git
   cd bookmark-manager
   ```

2. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

3. **进行修改**
   - 遵循现有的代码风格
   - 添加必要的注释
   - 确保代码可以编译运行

4. **测试你的修改**
   ```bash
   # 前端
   npm run build

   # 后端
   cargo check --manifest-path=src-tauri/Cargo.toml
   cargo build --manifest-path=src-tauri/Cargo.toml

   # 运行应用
   npm run tauri dev
   ```

5. **提交修改**
   ```bash
   git add .
   git commit -m "feat: 添加新功能描述"
   # 或
   git commit -m "fix: 修复某个问题"
   ```

6. **推送到 GitHub**
   ```bash
   git push origin feature/your-feature-name
   ```

7. **创建 Pull Request**
   - 在 GitHub 上创建 PR
   - 清晰描述你的修改
   - 链接相关的 issue

## 代码规范

### Rust 代码

- 使用 `cargo fmt` 格式化代码
- 使用 `cargo clippy` 检查代码质量
- 遵循 Rust 命名约定

### TypeScript/React 代码

- 使用有意义的变量名
- 组件名使用 PascalCase
- 函数名使用 camelCase
- 添加必要的类型注解

### Git Commit 规范

使用语义化的 commit 消息：

- `feat:` 新功能
- `fix:` Bug 修复
- `docs:` 文档更新
- `style:` 代码格式（不影响功能）
- `refactor:` 重构
- `test:` 测试相关
- `chore:` 构建/工具相关

示例：
```
feat: 添加导出书签功能
fix: 修复提醒时间不准确的问题
docs: 更新 README 安装说明
```

## 开发环境设置

### 前置要求

- Rust 1.93+
- Node.js 20+
- npm 或 yarn

### 安装依赖

```bash
# 安装前端依赖
npm install

# Rust 依赖会在构建时自动安装
```

### 运行开发服务器

```bash
npm run tauri dev
```

### 构建生产版本

```bash
npm run tauri build
```

## 项目结构

```
bookmark-manager/
├── src/                    # React 前端
│   ├── App.tsx            # 主组件
│   ├── api.ts             # API 调用
│   ├── types.ts           # 类型定义
│   └── App.css            # 样式
├── src-tauri/             # Rust 后端
│   ├── src/
│   │   ├── main.rs        # 入口
│   │   ├── lib.rs         # 主逻辑
│   │   ├── database.rs    # 数据库
│   │   ├── models.rs      # 数据模型
│   │   ├── commands.rs    # Tauri 命令
│   │   ├── reminder.rs    # 提醒服务
│   │   └── import.rs      # 导入功能
│   └── Cargo.toml         # Rust 依赖
└── package.json           # Node 依赖
```

## 需要帮助？

如果你有任何问题：

- 查看现有的 issues
- 创建新的 issue 提问
- 查看项目文档

## 行为准则

- 尊重所有贡献者
- 保持友好和专业
- 接受建设性的批评
- 关注对项目最有利的事情

感谢你的贡献！🎉
